spring:
  application:
    name: zero-downtime-deployment
  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  port: 8080
  shutdown: graceful
  env: ${APP_COLOR:local}

app:
  color: ${APP_COLOR:local}
